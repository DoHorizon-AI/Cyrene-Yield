# Module documentation / 模块文档

This directory maps Yield's Product core and its Plugins-owned capability seams.

本目录梳理 Yield 的 Product 核心与 Plugins 能力接缝。

| Directory | Responsibility / 职责 |
|---|---|
| [`yield/`](yield/README.md) | Product-level module map / 产品级模块地图 |

## Suggested reading order / 推荐阅读顺序

Read `yield/README.md`, then choose `core/` for lifecycle contracts or `plugin/` for the external LLaMA Factory connection boundary.

阅读 `yield/README.md`，再根据需要选择 `core/`（生命周期契约）或 `plugin/`（外部 LLaMA Factory 连接边界）。
---

<!-- Chinese Translation / 中文翻译 -->

# 模块文档

本目录梳理 Yield 的 Product core 及 Plugins 所有的能力接缝。

| 目录 | 职责 |
|---|---|
| yield/ | Product 级模块地图 |

## 推荐阅读顺序

阅读 yield/README.md，然后根据需要选择 core/ 了解生命周期契约，或选择 plugin/ 了解外部 LLaMA Factory 连接边界。
