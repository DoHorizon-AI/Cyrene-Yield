# training/core/src / training/core/src

Training-core Python source root.

训练核心 Python 源码根目录。

## Files / 文件

| Entry | Responsibility / 职责 |
|---|---|
| `training/core/src/cy_exec/` | Unified Yield training package. / 统一 Yield 训练包。 |

## Suggested reading order / 推荐阅读顺序

Start with `training/core/src/cy_exec/` and then follow the package entry point or imports.
Read sibling modules in runtime order; use the parent README for ownership boundaries.
从 `training/core/src/cy_exec/` 开始，再按包入口或导入关系继续阅读。
按运行时顺序阅读同级模块；职责边界请查阅父目录 README。
---

<!-- Chinese Translation / 中文翻译 -->

# training/core/src

训练核心 Python 源码根目录。

## 文件

| 条目 | 职责 |
|---|---|
| training/core/src/cy_exec/ | 统一 Yield 训练 package。 |

## 推荐阅读顺序

先从 training/core/src/cy_exec/ 开始，再沿包入口或导入关系继续阅读。
按 runtime 顺序阅读同级模块；职责边界参见父目录 README。
